package vn.hust.huy.backend.model.enums;

/**
 * Type of entry in the dictionary.
 * Maps to PostgreSQL enum type {@code entry_type_enum}.
 */
public enum EntryType {
    word,
    kanji,
    grammar
}
